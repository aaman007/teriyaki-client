export const createOrder = (token, orderData) => dispatch => {
    // fetch()
};

export const fetchOrders = token => dispatch => {
    // fetch()
};

export const fetchOrder = (token, orderId) => dispatch => {
    // fetch()
};